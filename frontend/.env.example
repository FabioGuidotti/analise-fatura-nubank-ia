# URL base da API. Em desenvolvimento, deixe vazio para usar o proxy do Vite (/api).
VITE_API_URL=
# Alvo do proxy de desenvolvimento (usado pelo vite.config.js)
VITE_API_TARGET=http://localhost:8000
